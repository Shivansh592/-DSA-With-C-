#include <iostream>
using namespace std;

int main() {
    int a;
    scanf("%d", &a);

    if (a >= 18) {
        cout << "You can vote." << endl;
    } else {
        cout << "You can't vote." << endl;
    }
    return 0;
}
